# Runtime dependencies for PyMusic (Python 3).
#
# Mandatory
SQLAlchemy>=2.0
mutagen>=1.47
jeepney>=0.8  # MPRIS / D-Bus integration (Linux)

# GUI backends (install whichever matches the entry point you want to use)
#   run.py       -> wxPython
#   run.gtk.py   -> PyGObject (GTK 3)
wxPython>=4.2
PyGObject>=3.42

# Audio backend. Default backend is player.vlc_backend (requires VLC from the
# system, e.g. `apt install vlc`). Alternatives: player.gstreamer (needs
# PyGObject + GStreamer system libs), player.mp3player (pygame-based, limited).
python-vlc>=3.0
pygame>=2.1

# Visualisation (Lindenmayer fractal pencil)
numpy>=1.23
